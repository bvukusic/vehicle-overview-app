import { VehicleNameFormatPipe } from './vehicle-name-format.pipe';

describe('VehicleNameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleNameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
